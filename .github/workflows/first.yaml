name: First_Workflow

on: workflow_dispatch

jobs:
  
  First_Job:
    name: "First Job"
    runs-on: ubuntu-latest
    steps:
      - name: "First Step"
        run: |
           echo "My First Step"
        shell: bash
